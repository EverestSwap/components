// the Everest Default token list lives here
export const ICE_LIST = 'https://raw.githubusercontent.com/EverestSwap/tokenlist/main/lists/mainnet.tokenlist.json';

export const DEFAULT_TOKEN_LISTS_SELECTED: string[] = [ICE_LIST];

export const DEFAULT_TOKEN_LISTS: string[] = [ICE_LIST];

export const AEB_TOKENS: string[] = [];
